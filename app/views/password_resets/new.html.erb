<%= provide(:title, "Forgot password") %>
<h1>Forgot password</h1>
<p>Essa é uma página para recuperar a senha de usuários</p>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(url: password_resets_path, scope: :password_reset) do |f| %>
      <%= f.label :email %>
      <%= f.email_field :email, class: "form-control" %>
      
      <%= f.submit "Recuperar minha senha", class: "btn btn-primary" %>
    <% end %>

    <p>Caso não tenha uma conta, <%= link_to "cadastre-se agora", 
      signup_path %>!</p>
  </div>
</div>