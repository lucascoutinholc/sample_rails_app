<%= provide(:title, "Forgot password") %>
<h1>Forgot password</h1>
<p>Essa é uma página para redefinição de senha dos usuários</p>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(model: @user, 
                  url: password_reset_path(params[:id])) do |f| %>

      <%= render "shared/error_messages" %>

      <%= hidden_field_tag :email, @user.email %>

      <%= f.label :password, "Senha" %>
      <%= f.password_field :password, class: "form-control" %>

      <%= f.label :password_confirmation, "Confirmação de Senha" %>
      <%= f.password_field :password_confirmation, class: "form-control" %>

      <%= f.submit "Redefinir minha senha", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>