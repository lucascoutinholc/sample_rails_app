<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "Excluir usuário", user, class: "delete", data: { 
        "turbo-method": :delete, turbo_confirm: "Você tem certeza?"
      } %>
  <% end %>
</li>