<%= provide(:title, "Users") %>
<h1>Usuários</h1>
<p>Aqui você encontra uma lista com todos os usuários cadastrados no sistema</p>

<div class="flickr_pagination">
  <%= will_paginate %>

  <ul class="users">
    <% @users.each do |user| %>
      <li>
        <%= gravatar_for user, size: 50 %>
        <%= link_to user.name, user %>
      </li>
    <% end %>
  </ul>
</div>